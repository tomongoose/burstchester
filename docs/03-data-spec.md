# 데이터셋 포맷 명세

## 1. 원칙
1. **새로운 포맷을 발명하지 않는다.** 이미 표준화된 ShareGPT / OpenAI messages JSONL을 그대로 쓴다.
2. **한 줄 = 한 학습 샘플** (JSONL).
3. **메타데이터는 별도 파일** (`meta.json`)에 둔다. 학습 데이터에 섞지 않는다.

---

## 2. 지원 포맷

### 2-1. OpenAI messages (권장 디폴트)
가장 널리 받아들여지는 포맷. TRL, Unsloth, Axolotl 모두 지원.

```jsonl
{"messages":[{"role":"system","content":"You are a helpful assistant."},{"role":"user","content":"한국 헌법 1조는?"},{"role":"assistant","content":"대한민국은 민주공화국이다."}]}
{"messages":[{"role":"user","content":"..."},{"role":"assistant","content":"..."}]}
```

**제약:**
- `role`: `system` | `user` | `assistant` | `tool` 만 허용
- 첫 메시지가 `assistant`이면 거부
- 마지막 메시지는 반드시 `assistant`
- 한 샘플 토큰 합계 ≤ 32k (대부분 base model의 컨텍스트 한계)

### 2-2. ShareGPT (대화 데이터셋용)
다중턴 롤플레이 / 대화에 흔히 쓰임.

```jsonl
{"conversations":[{"from":"human","value":"..."},{"from":"gpt","value":"..."}]}
```

매핑: `human` → `user`, `gpt` → `assistant`. 업로드 시 자동으로 OpenAI messages로 변환·저장하고 원본은 보관.

### 2-3. Alpaca (instruction tuning용, 단일턴만)
```jsonl
{"instruction":"...","input":"...","output":"..."}
```

내부적으로 `system="You are a helpful assistant."` + `user=instruction\n\ninput` + `assistant=output`로 변환.

---

## 3. 다운로드 zip 구조

사용자가 데이터셋 페이지에서 "Download" 누르면 받게 되는 zip:

```
my-dataset_v1.zip
├── dataset.jsonl          # OpenAI messages 포맷 (정규화됨)
├── dataset.sharegpt.jsonl # ShareGPT 포맷 (호환용, 옵션)
├── meta.json              # 메타데이터
├── Modelfile.template     # Ollama 템플릿
├── README.md              # 학습 가이드 (Colab 링크 포함)
└── LICENSE                # 데이터셋 라이선스
```

### `meta.json` 예시
```json
{
  "id": "abc123",
  "title": "Korean legal Q&A — small",
  "description": "...",
  "owner": { "uid": "...", "name": "..." },
  "tags": ["korean", "legal", "instruction"],
  "baseModelHint": "qwen2.5:7b",
  "taskType": "instruction",
  "format": "openai-messages",
  "language": "ko",
  "license": "CC-BY-4.0",
  "stats": { "rows": 1234, "avgUserTokens": 87, "avgAssistantTokens": 312 },
  "createdAt": "2026-05-04T...",
  "burstchester": { "version": "1.0", "url": "https://burstchester.app/d/abc123" }
}
```

### `Modelfile.template` 예시
```Dockerfile
# Ollama Modelfile — generated by Burstchester
# Replace {{GGUF_PATH}} with the path to your quantized merged model.
FROM {{GGUF_PATH}}

PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER stop "<|im_end|>"

TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>
"""

SYSTEM """You are a model fine-tuned on the "{{TITLE}}" dataset from Burstchester."""
```

---

## 4. 업로드 시 검증 파이프라인

`onDatasetUpload` Cloud Function이 수행:

1. **MIME / 확장자** — `.jsonl` 만 허용, ≤ 100MB
2. **라인별 JSON 파싱** — 첫 1만 라인 + 랜덤 샘플 1만
3. **포맷 정합성** — messages 배열 / role 화이트리스트 / 빈 content 거부
4. **PII 정규식 스캔** — 이메일, 주민번호 패턴, 전화번호, 신용카드, API 키 패턴 → 발견 시 status=`pending_review`
5. **언어 감지** — `franc` 사용. 사용자 입력과 mismatch면 경고
6. **중복 검사** — 라인의 SHA256 → 50% 이상 중복이면 거부
7. **통계 집계** — rowCount, avgTokens, byteSize → Firestore 메타 갱신

검증 실패 → Firestore에 `status: "rejected"` + `rejectReason` 기록. 사용자에게 메일 발송 (Phase 1.5).

---

## 5. 메타데이터 권장 태그 분류

UI 단의 **카테고리 트리** (Phase 1):

```
domain/        → legal, medical, finance, code, academic, creative, gaming
language/      → ko, en, ja, multi
task/          → instruction, chat, roleplay, summarization, translation, tool-use, code-review
base-model/    → llama3.1, qwen2.5, mistral, phi3, gemma2
size/          → tiny(<100), small(<1k), medium(<10k), large(>=10k)
quality/       → seed (운영자 큐레이션), community
```

자유 태그도 허용하지만 **자동완성에서 위 분류를 우선 노출**.

---

## 6. 라이선스 정책

### 6-1. 데이터셋 자체의 라이선스

| 라이선스 | 허용 |
|---|---|
| CC0 / Public Domain | ✅ |
| CC-BY / CC-BY-SA | ✅ (출처 명시 의무) |
| MIT / Apache-2.0 | ✅ |
| OpenAI/Anthropic ToS 산출물 | ❌ — 업로드 거부 (사용자 자기신고 + 약관) |
| 사용자 정의 | ⚠️ 경고 후 허용 |

**모든 다운로드 zip의 `LICENSE` 파일에 원본 라이선스 + Burstchester 약관 명시.**

---

## 7. 데이터 출처(Source Model) 정책

데이터셋이 **어떤 모델 또는 사람의 출력에서 왔는지**를 메타데이터로 강제. 업로드 폼에 `sourceModel` 필드를 **필수**로 받고, 화이트리스트 외 값이면 reject.

### 7-1. ✅ 권장 출처 (기본 허용) — Apache 2.0 / MIT

출력물을 그대로 데이터셋으로 만들어 공유·재학습 가능.

| 모델 | 제공자 | 라이선스 | 비고 |
|---|---|---|---|
| **Qwen 2.5 / Qwen 3** (0.5B~72B) | Alibaba | Apache 2.0 | 다국어 강력, 한국어 양호. 커뮤니티 1순위 |
| **Mistral 7B / Mixtral 8x7B / 8x22B** | Mistral AI | Apache 2.0 | 초기 오픈소스 표준. 단, **Mistral Large/Medium API는 ❌** |
| **DeepSeek V2 / V3 base** | DeepSeek | MIT (가중치) | base 모델은 안전. instruct는 별도 확인 |
| **Phi-3 / Phi-4** | Microsoft | MIT | 소형(3.8B~14B), 영어 강함 |
| **OLMo 2** | AI2 | Apache 2.0 | 학습 데이터까지 공개 — 가장 깨끗 |
| **Pythia / GPT-NeoX** | EleutherAI | Apache 2.0 | 연구용 표준 |
| **SmolLM 2** | HuggingFace | Apache 2.0 | 소형 디바이스용 |
| **Falcon 3** | TII | Apache 2.0 | |

### 7-2. ⚠️ 조건부 허용 — 커뮤니티 라이선스

업로드 가능하지만 **출처 태그 강제 + 약관 명시**.

| 모델 | 라이선스 | 주의 사항 |
|---|---|---|
| **Llama 3.1 / 3.2 / 3.3** | Llama Community License | 파생 모델 이름에 "Llama" 접두 의무, MAU 7억 초과 시 별도 라이선스 |
| **Gemma 2 / Gemma 3** | Gemma Terms of Use | Google "Prohibited Use Policy" 준수 의무 |
| **Yi / Yi-1.5** | Apache 2.0 (현재) | 과거 제약 있었으나 현재 풀림 |

### 7-3. ❌ 거부 — 비공개/비상업 모델 출력

ToS에서 "출력물을 경쟁 모델 학습에 사용 금지" 조항 명시.

| 서비스 | 금지 근거 |
|---|---|
| **OpenAI** (ChatGPT, GPT-4o, o1, o3, o4) | Terms of Use §2(c): *develop models that compete with OpenAI* |
| **Anthropic Claude** | Usage Policy: *train models that compete* |
| **Google Gemini** (API/앱) | Additional Terms: *develop machine learning models* |
| **xAI Grok** | ToS: *train any AI model* |
| **Mistral Large / Medium (API)** | API 출력 학습 금지 + 가중치 비공개 |
| **Cohere Command R / R+** | CC-BY-NC 4.0 (비상업) |

→ 업로드 시 자동 거부 + 신고 시스템.

### 7-4. ✅ 사람이 직접 작성한 데이터

사용자 본인이 작성했거나 라이선스 깨끗한 사람-작성 데이터(예: 위키, 공개 Q&A)는 항상 허용. `sourceModel: "human"` 으로 기록.

### 7-5. 2026-05 기준 커뮤니티에서 가장 많이 파인튜닝되는 모델 (참고용 순위)

1. **Qwen 2.5 / Qwen 3** (7B, 14B) — 압도적 1위
2. **Llama 3.1 8B / 3.2 3B**
3. **Mistral 7B / Nemo 12B**
4. **Gemma 2 9B / Gemma 3**
5. **Phi-4 14B**
6. **DeepSeek V3 base**

→ 업로드 폼의 `baseModelHint` 자동완성 우선 노출 순서로 사용.

### 7-6. 메타 필드 추가 (Firestore `datasets/{id}`)

```ts
sourceModel: "qwen3:14b" | "llama3.1:8b" | "human" | string,  // 화이트리스트 검증
sourceModelLicense: "apache-2.0" | "mit" | "llama-community" | "gemma-tou" | "human" | "other",
sourceConfirmed: boolean,  // 업로드 시 약관 동의 체크박스
```

`onDatasetUpload` Function에서 `sourceModel`이 7-3 블랙리스트에 매칭되면 즉시 reject. 7-1/7-2 화이트리스트가 아니면 `status: "pending_review"`로 보류.
