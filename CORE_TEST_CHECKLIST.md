# Bustchester Core Manual Test Guide

이 문서는 릴리스 전 사람이 반복해서 확인하는 핵심 수동 테스트 매뉴얼이다.
각 항목은 실제 사용자 흐름을 기준으로 실행하고, 실패한 항목은 재현 경로와
화면/CLI 출력 증거를 남긴다.

## 테스트 기록 양식

- 테스트 일시:
- 테스트한 배포 URL:
- 테스트 계정:
- 사용한 CLI 버전 또는 커밋:
- 결과: Pass / Fail
- 실패 항목:
- 비고:

## 공통 준비

1. 웹 배포 URL에 접속한다.
2. 테스트용 Google 계정 또는 익명 계정을 준비한다.
3. CLI가 설치되어 있고 `burstchester` 명령을 실행할 수 있는지 확인한다.
4. 테스트용 access token을 준비하거나 웹에서 새로 발급한다.
5. 테스트에 사용할 데이터셋 ID, 모델 ID, 출력 디렉터리를 기록한다.
6. 포인트 검증이 필요한 경우 테스트 전 사용자 포인트를 기록한다.

## 1. 웹 로그인과 Access Token

목표: 웹 인증, 프로필 생성, CLI access token 관리가 정상인지 확인한다.

- [ ] 웹에서 익명 로그인 또는 Google 로그인을 수행한다.
- [ ] 로그인 후 프로필 페이지가 열리고 사용자 정보가 표시되는지 확인한다.
- [ ] 프로필에 닉네임, 포인트, 업로드 수, 다운로드 수가 표시되는지 확인한다.
- [ ] Access Token 페이지에서 새 CLI access token을 발급한다.
- [ ] 발급 결과에 token ID와 실제 token 값이 표시되는지 확인한다.
- [ ] 화면을 새로고침한 뒤 실제 token 전체 값이 다시 노출되지 않는지 확인한다.
- [ ] 발급된 token 목록에 방금 만든 token ID가 표시되는지 확인한다.
- [ ] token을 삭제한 뒤 목록에서 사라지는지 확인한다.
- [ ] 삭제된 token으로 CLI 인증 요청을 실행했을 때 실패하는지 확인한다.
- [ ] 로그아웃 후 token 발급, token 목록 조회, 프로필 수정이 차단되는지 확인한다.

## 2. 데이터셋 탐색

목표: 데이터셋 목록, 필터, 상세 보기, 업로더 표시가 정상인지 확인한다.

- [ ] Explore 페이지에서 Datasets 탭을 선택한다.
- [ ] 데이터셋 카드 목록이 로드되는지 확인한다.
- [ ] 카드에 제목, 설명, 태그, 크기, 좋아요 수, 다운로드 수가 표시되는지 확인한다.
- [ ] 비익명 업로더라면 닉네임 옆에 프로필 이미지가 표시되는지 확인한다.
- [ ] 익명 업로더는 프로필 링크와 프로필 이미지가 표시되지 않는지 확인한다.
- [ ] Domain 필터를 선택했을 때 목록이 해당 도메인 중심으로 바뀌는지 확인한다.
- [ ] Language 필터에서 일본 코드는 `jp`로 표시되는지 확인한다.
- [ ] Task, Base model, Size 필터가 클릭/해제되는지 확인한다.
- [ ] Most popular / Most recent 정렬을 전환했을 때 목록이 갱신되는지 확인한다.
- [ ] 데이터셋 카드를 클릭하면 상세 패널이 열리는지 확인한다.
- [ ] 상세 패널에 제목, 설명, 태그, 크기, 다운로드 수, 데이터셋 ID가 표시되는지 확인한다.
- [ ] 상세 패널에서 다운로드 액션이 표시되는지 확인한다.
- [ ] 목록 조회 실패 상황에서는 빈 화면이 아니라 오류 또는 대체 상태가 표시되는지 확인한다.

## 3. 모델 탐색

목표: 모델 목록, 모델 전용 필터, 상세 보기가 데이터셋 탐색과 같은 방식으로 동작하는지 확인한다.

- [ ] Explore 페이지에서 Models 탭을 선택한다.
- [ ] 모델 카드 목록이 로드되는지 확인한다.
- [ ] 카드에 모델 ID, 베이스 모델, 학습 방식, 학습 데이터셋 수, 포인트 가격이 표시되는지 확인한다.
- [ ] 비익명 모델 소유자 닉네임이 프로필 링크로 표시되는지 확인한다.
- [ ] 익명 모델 소유자는 프로필 링크가 표시되지 않는지 확인한다.
- [ ] Training 필터에서 `lora`, `qlora`, `full`을 선택/해제할 수 있는지 확인한다.
- [ ] Base model 필터를 선택/해제할 수 있는지 확인한다.
- [ ] 필터 적용 후 모델 목록이 갱신되는지 확인한다.
- [ ] 모델 카드를 클릭하면 모델 상세 패널로 이동하는지 확인한다.
- [ ] 모델 상세 패널에 모델 ID, 베이스 모델, 학습 데이터셋 수, 포인트 가격, 업데이트 날짜가 표시되는지 확인한다.
- [ ] 상세 패널의 Hugging Face 링크가 새 탭 또는 외부 링크로 정상 이동하는지 확인한다.
- [ ] Ollama pull URL이 있는 모델은 명령 문자열이 표시되는지 확인한다.
- [ ] 존재하지 않는 모델 ID로 상세 URL에 접근했을 때 “Model not found” 상태가 표시되는지 확인한다.

## 4. CLI Access Token 연동

목표: 웹에서 발급한 access token이 CLI 인증에 사용할 수 있는지 확인한다.

- [ ] 웹에서 발급한 token을 CLI `--access-token` 값으로 전달한다.
- [ ] 같은 token을 `BURSTCHESTER_ACCESS_TOKEN` 환경 변수로 전달한다.
- [ ] 유효한 token으로 인증이 필요한 CLI 명령이 성공하는지 확인한다.
- [ ] 잘못된 token, 폐기된 token, 빈 token은 명확한 오류와 함께 거부되는지 확인한다.
- [ ] CLI 출력에 민감한 token 전체 값이 불필요하게 다시 표시되지 않는지 확인한다.
- [ ] 로컬 access-token 파일을 사용하는 흐름이 있다면 해당 파일에서 token을 읽는지 확인한다.

## 5. CLI 데이터셋 리스트 관리

목표: 로컬 데이터셋 ID 목록을 추가, 조회, 수정, 재사용할 수 있는지 확인한다.

- [ ] `dataset-list add --dataset-id <id>`로 데이터셋 ID를 추가한다.
- [ ] 같은 ID를 다시 추가해도 중복 저장되지 않는지 확인한다.
- [ ] `dataset-list show`가 현재 저장된 ID 목록을 출력하는지 확인한다.
- [ ] `dataset-list remove --dataset-id <id>`로 특정 ID를 삭제한다.
- [ ] 삭제 후 `dataset-list show`에 해당 ID가 없는지 확인한다.
- [ ] `dataset-list clear`로 전체 리스트를 초기화한다.
- [ ] 파일에서 데이터셋 ID 목록을 import할 수 있는지 확인한다.
- [ ] 현재 데이터셋 ID 목록을 파일로 export할 수 있는지 확인한다.
- [ ] export한 파일을 다른 명령 입력으로 재사용할 수 있는지 확인한다.
- [ ] 존재하지 않는 데이터셋 ID를 학습 또는 다운로드에 사용할 때 명확히 실패하는지 확인한다.

## 6. CLI 데이터셋 다운로드

목표: 데이터셋 다운로드 준비, ZIP 다운로드, 압축 해제 결과물이 정상인지 확인한다.

- [ ] `download-dataset --dataset-id <id> --access-token <token>`을 실행한다.
- [ ] 다운로드 준비 응답에 signed URL, zip path, cache 상태가 포함되는지 확인한다.
- [ ] signed URL로 ZIP 파일이 다운로드되는지 확인한다.
- [ ] 다운로드된 ZIP 파일의 무결성 검사가 성공하는지 확인한다.
- [ ] 기본 설정에서 ZIP 압축 해제가 성공하는지 확인한다.
- [ ] 해제된 디렉터리에 `dataset.jsonl`이 포함되는지 확인한다.
- [ ] 해제된 디렉터리에 `dataset.sharegpt.jsonl`이 포함되는지 확인한다.
- [ ] 해제된 디렉터리에 `meta.json`, `README.md`, `LICENSE`, `Modelfile.template`이 포함되는지 확인한다.
- [ ] `--extract false` 사용 시 ZIP만 저장되고 압축 해제는 수행되지 않는지 확인한다.
- [ ] `--out-dir <dir>` 사용 시 지정 경로에 결과물이 저장되는지 확인한다.

## 7. CLI 파인튜닝

목표: 단일/복수 데이터셋을 사용한 학습 준비와 실행 흐름이 정상인지 확인한다.

- [ ] CLI에서 베이스 모델 또는 모델 repo 입력값을 전달한다.
- [ ] 단일 dataset ID를 학습 입력으로 사용한다.
- [ ] 저장된 dataset-list를 학습 입력으로 사용한다.
- [ ] 파일로 전달한 데이터셋 리스트를 학습 입력으로 사용한다.
- [ ] 학습 전 필요한 데이터셋들이 다운로드되는지 확인한다.
- [ ] 여러 데이터셋이 학습 가능한 형식으로 병합되는지 확인한다.
- [ ] 학습 workspace가 생성되는지 확인한다.
- [ ] workspace에 입력 데이터, 설정, 로그가 저장되는지 확인한다.
- [ ] preflight 모드에서 다운로드, 입력 검증, workspace 생성까지만 수행되는지 확인한다.
- [ ] 실제 파인튜닝 명령이 베이스 모델과 데이터셋 입력을 올바르게 전달하는지 확인한다.
- [ ] 학습 완료 후 체크포인트, 어댑터, 메타데이터 등 산출물이 생성되는지 확인한다.
- [ ] 모델 입력값, 데이터셋 입력값, token, workspace 경로 누락 시 명확한 오류가 표시되는지 확인한다.

## 8. 모델 업로드와 레지스트리 등록

목표: 파인튜닝 결과 모델을 업로드하고 웹 모델 탐색에서 확인할 수 있는지 검증한다.

- [ ] 파인튜닝 결과물을 Hugging Face 또는 지정 저장소에 업로드한다.
- [ ] 업로드 성공 후 다운로드 가능한 모델 URL을 얻는다.
- [ ] 업로드한 모델을 백엔드 모델 레지스트리에 등록한다.
- [ ] 등록 요청에 모델명, 베이스 모델, 학습 방식, 관련 dataset ID, 다운로드 URL이 포함되는지 확인한다.
- [ ] Hugging Face 다운로드 URL 형식이 아닌 URL은 등록이 거부되는지 확인한다.
- [ ] 등록된 모델이 웹 Models 목록에 표시되는지 확인한다.
- [ ] 등록된 모델 카드 클릭 시 상세 패널이 열리는지 확인한다.
- [ ] 상세 패널에서 업로드된 모델 URL로 이동할 수 있는지 확인한다.
- [ ] 동일 모델 중복 등록 정책이 의도대로 동작하는지 확인한다.

## 9. 모델 입출력 캡처와 데이터셋 업로드

목표: 모델 사용 로그를 재학습 가능한 데이터셋으로 전환할 수 있는지 확인한다.

- [ ] 모델 추론 중 사용자 입력 prompt와 모델 출력 response를 저장한다.
- [ ] 저장된 입출력 로그가 JSONL 등 학습 가능한 포맷인지 확인한다.
- [ ] 저장 파일에 source model, 생성 시각, 세션 식별자 등 메타데이터가 포함되는지 확인한다.
- [ ] CLI에서 저장된 입출력 로그 파일을 업로드한다.
- [ ] 업로드된 입출력 로그가 신규 데이터셋으로 등록되는지 확인한다.
- [ ] 등록된 입출력 데이터셋이 웹 Datasets 목록에 표시되는지 확인한다.
- [ ] 업로드된 입출력 데이터셋을 dataset-list에 추가할 수 있는지 확인한다.
- [ ] 업로드된 입출력 데이터셋을 후속 파인튜닝 입력으로 사용할 수 있는지 확인한다.
- [ ] 민감정보가 포함된 로그에 대한 검토 또는 차단 정책이 적용되는지 확인한다.

## 10. 포인트와 보상 정책

목표: 다운로드 비용, 보상 포인트, 중복 구매 정책이 일관되게 반영되는지 확인한다.

- [ ] 테스트 전 구매자와 소유자의 포인트를 기록한다.
- [ ] 데이터셋 다운로드 시 구매자 포인트가 `pointCost`만큼 차감되는지 확인한다.
- [ ] 데이터셋 다운로드 시 데이터셋 소유자에게 보상 포인트가 지급되는지 확인한다.
- [ ] 구매자 차감, 소유자 지급, 구매 기록, 다운로드 카운트가 함께 저장되는지 확인한다.
- [ ] 이미 구매한 데이터셋 재다운로드 시 중복 차감되지 않는지 확인한다.
- [ ] 포인트가 부족한 사용자는 다운로드 URL을 발급받을 수 없는지 확인한다.
- [ ] 본인 소유 데이터셋 다운로드 시 차감/지급 정책이 의도대로 적용되는지 확인한다.
- [ ] 모델 다운로드에도 `pointCost`가 있다면 구매자 포인트가 차감되는지 확인한다.
- [ ] 모델 다운로드에도 소유자 보상 정책이 있다면 보상 포인트가 지급되는지 확인한다.
- [ ] access token을 통한 CLI 다운로드에도 웹 다운로드와 같은 포인트 정책이 적용되는지 확인한다.
- [ ] 포인트 거래 내역에 사용자, asset type, asset ID, 비용, 잔액, 시각이 기록되는지 확인한다.
- [ ] 정산 중 오류가 발생하면 부분 차감이나 부분 지급 없이 실패하는지 확인한다.

## 11. 포인트 정책 페이지

목표: `/points` 페이지가 테스트 오픈 상태와 향후 결제/현금화 계획을 명확히 설명하는지 확인한다.

- [ ] `/points` 페이지에 접속한다.
- [ ] Gemma 4 Good Hackathon 테스트 오픈 기간임이 표시되는지 확인한다.
- [ ] 결제, 유료 체크아웃, 현금 반환/출금 기능이 아직 비활성화되어 있다고 명시되는지 확인한다.
- [ ] 다운로드가 업로더 보상 신호가 된다는 설명이 있는지 확인한다.
- [ ] 충분한 포인트가 쌓였을 때 추후 현금화 요청이 가능해질 계획이 설명되는지 확인한다.
- [ ] 포인트가 참여 유도와 모델/데이터셋 신뢰도 향상에 어떻게 기여하는지 문단별로 설명되는지 확인한다.

## 12. 릴리스 스모크 플로우

목표: 릴리스 직전 핵심 경로를 짧게 연결 실행한다.

- [ ] 웹 로그인 후 access token을 발급한다.
- [ ] CLI에서 발급 token으로 인증이 필요한 명령을 실행한다.
- [ ] 웹에서 데이터셋 카드와 모델 카드를 탐색한다.
- [ ] 데이터셋 상세 보기와 모델 상세 보기를 각각 연다.
- [ ] CLI에서 데이터셋 리스트를 추가, 조회, export한다.
- [ ] CLI에서 데이터셋 리스트를 사용해 파인튜닝 preflight를 실행한다.
- [ ] CLI에서 실제 파인튜닝을 실행해 산출물을 생성한다.
- [ ] 파인튜닝한 모델을 업로드하고 모델 레지스트리에 등록한다.
- [ ] 등록된 모델이 웹 모델 목록과 상세 보기에서 확인되는지 확인한다.
- [ ] 모델 입출력 로그를 저장하고 데이터셋으로 업로드한다.
- [ ] 업로드된 입출력 데이터셋이 웹에 표시되고 재학습 입력으로 사용되는지 확인한다.
- [ ] 데이터셋 또는 모델 다운로드 시 포인트 차감과 소유자 보상이 정상 반영되는지 확인한다.

## 실패 기록 규칙

실패 항목이 있으면 아래 정보를 남긴다.

- 실패한 섹션과 체크 항목:
- 재현 절차:
- 기대 결과:
- 실제 결과:
- 웹 URL 또는 CLI 명령:
- 관련 사용자 ID, dataset ID, model ID:
- 스크린샷 또는 CLI 출력:
- 임시 우회 방법:
